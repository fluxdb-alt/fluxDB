[package]
name = "fluxdb-storage"
version = "0.1.0"
edition = "2024"

[dependencies]
fluxdb-core = { path = "../fluxdb-core" }
rusqlite = { version = "0.32", features = ["bundled"] }
serde = { version = "1", features = ["derive"] }
serde_json = "1"
toml = "0.8"
