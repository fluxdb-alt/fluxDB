# PostgreSQL 接入 · 集中人工验收清单

建立日期：2026-09-12。本清单集中所有「需人工在 macOS 桌面交互确认」的验收项。
函数实现已完成、自动化测试已通过；亮/暗主题、弹框交互、鼠标手势等视觉/交互项需人工逐项确认。
按依赖顺序执行；每项确认后勾选并记录结果。最终一次性交付。

环境：`cargo run -p fluxdb-desktop`；连接隔离测试库 `fluxdb-t09-pg`（PG 16，127.0.0.1:5432 / postgres / secret / postgres）。
另备一个带多 schema / 大写混合命名 / 中文注释的库以覆盖跨 schema 与大小写场景。

> 通用通过标准：亮/暗两主题均正常；focus/hover 边框符合 AGENTS（明 0x111111、暗 0x8ab4ff）；
> 所有弹框 Esc / 外点 / 关闭按钮 / 内点不穿透；进度中有 loading/disabled。

---

## A. 连接对话框（T19）

| # | 前置 | 操作步骤 | 预期结果 |
|---|---|---|---|
| A1 | 桌面已启动 | 新建连接 → 类型下拉选 PostgreSQL | PG 卡片可选，基础/TLS/SSH/高级页签对 PG 开放 |
| A2 | A1 | TLS 选 verify-full 但未启用 TLS | 测试/保存被表单校验拒绝并提示「需先启用 TLS」 |
| A3 | A1 | verify-full + 纯 IP 主机 + 空白 server_name | 提示需主机名或 server_name |
| A4 | A1 | 填 127.0.0.1:5432/postgres/secret → 测试 | 测试成功；测试期间「测试」按钮 disabled + loading |
| A5 | A4 | 保存并连接 | 保存成功，侧栏出现 PG 连接，自动展开对象 |
| A6 | A4 | 编辑该连接 → TLS/SSH/证书/超时回填 | 表单与档案一致，证书路径不丢失 |
| A7 | A4 | 复制该连接 | 副本连接成功测试（独立密码），改副本密码不影响原连接 |
| A8 | A4 | 删除该连接 | 弹确认；有未保存/运行查询时拦截提示；删除后 Keychain 条目清理、对象树不残留 |
| A9 | 重启应用 | 重启后侧栏恢复该 PG 连接 | 恢复可用，编辑回填正确 |

## B. 对象树 / schema（T20）

| # | 前置 | 操作步骤 | 预期结果 |
|---|---|---|---|
| B1 | A5 | 展开连接 → 展开数据库 | 数据库 → 各 schema 行（public 等）独立成行 |
| B2 | B1 | 展开某 schema | schema 下 Table/View 分组 → 该 schema 的表；不同 schema 同名表互不串列 |
| B3 | B1 | 展开 schema 时快速切换/断开连接 | 迟到对象加载不覆盖新状态（无闪回/串表） |
| B4 | B1 | schema 行右键 | 独立 schema 菜单（新建查询/新建表/刷新/设置默认 schema），**非**数据库菜单 |
| B5 | B1 | 数据库右键 → 新建 schema | 弹框仅问名称（无 encoding/locale/template）；执行成功后新 schema 出现 |
| B6 | B1 | 数据库右键 → 新建数据库 | 弹框含编码/locale(LC)+Owner+模板；执行成功后新库出现 |
| B7 | B1 | 数据库右键 → 删除数据库 | 有运行/脏查询时拦截；失败不提前删节点；成功才清对象 |

## C. 查询 / 补全（T13/T14/T15）

| # | 前置 | 操作步骤 | 预期结果 |
|---|---|---|---|
| C1 | 已连库 | 新建查询，输入 SELECT * FROM 后触发补全 | 表/列按 search_path 顺序补全，注释作文档提示 |
| C2 | C1 | 补全含跨 schema 同名表 | 可跳 schema，同名不混淆 |
| C3 | C1 | 大查询执行 → 停止 | loading 有反馈，取消后不误报断连 |
| C4 | C1 | 执行 BEGIN→异常→ROLLBACK | 失败标注、提示需回滚，ROLLBACK 后恢复 |
| C5 | C1 | 查询结果编辑（单表简单 SELECT） | 可改并提交；JOIN/聚合等只读 |
| C6 | 有脏子句 | 关闭/切换带未保存 SQL 的查询标签 | 有脏标签保护提示 |
| C7 | 执行半途 | 断开连接 | 有运行/脏查询警告 |

## D. 数据 / 建表 / 表操作（T09–T12、T16–T18）

| # | 前置 | 操作步骤 | 预期结果 |
|---|---|---|---|
| D1 | B2 | 打开表数据页 | 分页、排序、过滤正常；numeric/decimal 保精、bytea 摘要、jsonb 可见 |
| D2 | D1 | 单元格详情打开 bytea 列 | 完整字节读取 |
| D3 | B2 | 重建表向导（schema 下） | PG 类型清单、identity、schema 限定 |
| D4 | B2 | 设计表 → 改列/加唯一索引 | 只发差异动作，预览=执行；外部 DDL 后保存被拒 |
| D5 | B2 | 复制/重命名/清空/删除表 | schema 限定、独立序列、确认弹框 |

## E. MySQL 回归（跨 T01–T28）

| # | 操作步骤 | 预期结果 |
|---|---|---|
| E1 | 新建/编辑 MySQL 连接、测试、保存 | 默认值（3306/root/utf8mb4/preferred）不变 |
| E2 | MySQL 建表/建库/查询/补全 | 与接入 PG 前行为一致（反引号、AUTO_INCREMENT 等） |
| E3 | 对象树 MySQL 层级 | 仍为 连接→库→分组，无 schema 行 |

## G. PG 用户/角色/权限（T26/T27）

| # | 操作步骤 | 预期结果 |
|---|---|---|
| G1 | PG 连接右键「用户与权限」 | 打开 PG 角色面板（非 MySQL user@host 占位）；列出集群角色，无 host/plugin/资源限制字段 |
| G2 | 点「新建角色」→ 填名/开 LOGIN/填密码 → 创建 | 创建成功；列表刷新出现新角色（含 LOGIN 用户与 NOLOGIN 组角色两种） |
| G3 | 选中普通角色 → 删除 | 删除成功；postgres/pg_* 无删除入口；有依赖时服务端 RESTRICT 拒绝并提示 |
| G4 | 选中角色 | 右侧显示其所属组角色（pg_auth_members）；postgres/pg_* 无改密/重命名入口 |
| G4b | 选中普通角色 → 点「重命名」→ 输入新名 → 确定 | 重命名成功并刷新列表；新名可登录登录；postgres/pg_* 无该入口 |
| G4c | 选中普通角色 → 点「改密」→ 输入新密码 → 确定 | 改密成功并提示；用新密码可登录；完成后编辑表单收起 |
| G4d | 重命名/改密表单 → 取消 | 表单收起，不做任何变更 |
| G4e | 选中普通 LOGIN 角色 → 关闭「可登录 (LOGIN)」开关 | 角色变为 NOLOGIN（离线）；列表刷新后开关反映 false；postgres/pg_* 开关只读 |
| G5 | 对象权限：选种类(表/视图/序列/schema/数据库/函数)+schema/对象(函数带签名) → 读取权限 | 显示属主；ACL 为 NULL 显示「属主 X（默认权限）」而非空表 |
| G6 | 对某权限点「授予」/「授予+可再授」 | 授权成功并刷新；该权限变为「直接授权（可撤销）」 |
| G7 | 对直接授权点「撤销」 | 仅撤销直接授权；继承/PUBLIC/属主权限显示为「不可直接撤销」且无撤销按钮 |
| G8 | 明暗主题、Esc/外点、按钮手形/hover、loading/disabled | 符合 AGENTS 主题与交互规范 |

## F. PG 原生备份与原生脚本（T24/T25，需主机有 psql/pg_dump/pg_restore）

> 后端子进程逻辑与 argv 已用容器内 psql/pg_dump 验证（PG16.15）；桌面端真实工具调用链
> 需主机带工具后人工确认。设置里可配 pg_dump 路径（设置 → 数据 → pg_dump 路径）。

| # | 操作步骤 | 预期结果 |
|---|---|---|
| F1 | PG 连接 → 备份 tab → 原生备份 | pg_dump 生成 .sql（plain+inserts）；进度、取消正常；密码不明文出现 |
| F2 | 用 psql 把备份恢复到干净隔离库 | 表/行/视图/索引/序列/函数齐全；序列位置正确；新增 identity 行不冲突 |
| F3 | 执行含 COPY FROM STDIN 的 SQL 文件 | 自动识别原生模式走 psql；COPY 数据正确落入；分号不误拆 |
| F4 | 原生脚本中途失败 | ON_ERROR_STOP 停止并提示失败；勾选「继续错误」则跳过继续 |
| F5 | 主机无 psql/pg_dump | 启动报「启动 psql/pg_dump 失败」清晰错误，不假成功 |
| F6 | TLS 连接（ssl_mode 非 prefer）执行原生备份/脚本 | psql/pg_dump 经 PGSSLMODE 以与连接器一致强度加密；不加密时明确报连接失败 |
| F7 | SSH 隧道连接执行原生备份/脚本 | 机制已实现并真库验证：`ssh -N -L 15432:<pg>:5432` 后 pg_dump(schema-only) 退出 0、psql 查询返回 1；备份/脚本路径均起隧道子进程并经 127.0.0.1:local 拨号（PGHOSTADDR），结束/取消 kill+wait 回收。桌面完整链路（真实 SSH 连接配置）待人工确认 |

---

## 结果记录

每项完成后把 `[ ]` 改 `[x]` 并附一句结果；全部完成后，由交付人据本清单勾选 T19/T20，并按依赖继续。（此文件本身即交付物。）
